export default function TodoCount({todos}){
    return(
        <>
            {todos.map((item)=>{
                return <span key={id}>{item}</span>
            })}
        </>
    )
}
import React from 'react'

