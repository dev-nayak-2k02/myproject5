import React from 'react'

const ItemList = ({item}) => {
  return (
    <div>
        {
            item.map((item)=>(
                <></>
            ))
        }
    </div>
  )
}

export default ItemList